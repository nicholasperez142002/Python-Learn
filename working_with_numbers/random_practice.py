#random (random module)
#functions - random(), randint()
#we use this to generate random data as dummy data

import random
print(random.random())
print(random.randint(1,6)) # random int from range 1 to 6